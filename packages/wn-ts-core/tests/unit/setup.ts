import { beforeAll, afterAll } from 'vitest';

// Global test setup
beforeAll(async () => {
  // Any global setup needed for tests
  console.log('Setting up WordNet Core tests...');
});

afterAll(async () => {
  // Any global cleanup needed for tests
  console.log('Cleaning up WordNet Core tests...');
});
